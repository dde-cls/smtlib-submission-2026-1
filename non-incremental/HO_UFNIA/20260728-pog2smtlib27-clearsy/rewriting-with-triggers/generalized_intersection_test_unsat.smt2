(set-info :smt-lib-version 2.7)
(set-logic HO_UFNIA)
(set-info :source "
Generated by: CLEARSY
Generated on: 2026-07-28
Generator: pog2smtlib-2.7
Application: generalized_intersection_test
Target solver: cvc5
Time limit: 0.31
Benchmarks crafted to test the proof obligation translator pog2smtlib-2.7 for Atelier B.

MACHINE
input_unsat
ASSERTIONS
inter({{1,2},{2,4}}) = {2}
END
")
(set-info :license "https://creativecommons.org/licenses/by/4.0/")
(set-info :category "crafted")
(set-info :status unknown)

;; type Z
(define-sort |Z| () Int)
;; Powerset constructor
(declare-sort P 1)
;; type POW Z
(define-sort |POW Z| () (P |Z|))
;; type POW POW Z
(define-sort |POW POW Z| () (P |POW Z|))
;; setmembership Z
(declare-fun |set.in Z| (|Z| |POW Z|) Bool)
;; setmembership POW Z
(declare-fun |set.in POW Z| (|POW Z| |POW POW Z|) Bool)
;; set Z
(define-sort |? Z| () (-> |Z| Bool))
(declare-const |set.intent Z| (-> |? Z| |POW Z|))
(assert (!
  (forall ((p |? Z|) (x |Z|)) (!
    (= (|set.in Z| x (|set.intent Z| p))
       (@ p x))
    :pattern ((|set.in Z| x (|set.intent Z| p)))))
  :named |ax.rw.intent Z|))
;; inter Z
(declare-fun |inter Z| (|POW POW Z|) |POW Z|)
(assert (!
  (forall ((E |POW POW Z|) (x |Z|)) (!
    (= (|set.in Z| x (|inter Z| E))
       (forall ((e |POW Z|)) (=> (|set.in POW Z| e E) (|set.in Z| x e))))
    :pattern ( (|set.in Z| x (|inter Z| E)) )))
  :named |ax.set.in.generalized.intersection Z|))
;; set POW Z
(define-sort |? POW Z| () (-> |POW Z| Bool))
(declare-const |set.intent POW Z| (-> |? POW Z| |POW POW Z|))
(assert (!
  (forall ((p |? POW Z|) (x |POW Z|)) (!
    (= (|set.in POW Z| x (|set.intent POW Z| p))
       (@ p x))
    :pattern ((|set.in POW Z| x (|set.intent POW Z| p)))))
  :named |ax.rw.intent POW Z|))
;; = Z
(assert (!
  (forall ((s |POW Z|) (t |POW Z|))
    (=
      (= s t)
      (forall ((e |Z|)) (= (|set.in Z| e s) (|set.in Z| e t)))))
  :named |ax.set.eq Z|))
(assert (!
    (not
      (= (|inter Z| (|set.intent POW Z| (lambda ((_c0 |POW Z|)) (or (= _c0 (|set.intent Z| (lambda ((_c1 |Z|)) (or (= _c1 1)(= _c1 2)))))(= _c0 (|set.intent Z| (lambda ((_c1 |Z|)) (or (= _c1 2)(= _c1 4))))))))) (|set.intent Z| (lambda ((_c0 |Z|)) (= _c0 2)))))
    :named |Goal|))
(check-sat)
(exit)
