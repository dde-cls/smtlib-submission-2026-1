(set-info :smt-lib-version 2.7)
(set-logic HO_UFNIA)
(set-info :source "
Generated by: CLEARSY
Generated on: 2026-07-28
Generator: pog2smtlib-2.7
Application: rdiv_test
Target solver: cvc5
Time limit: 0.31
Benchmarks crafted to test the proof obligation translator pog2smtlib-2.7 for Atelier B.

MACHINE
input_unsat
ASSERTIONS
5.0 / 2.0 = 2.5
END
")
(set-info :license "https://creativecommons.org/licenses/by/4.0/")
(set-info :category "crafted")
(set-info :status unsat)

;; type REAL
(define-sort |REAL| () Real)
;; rdiv
(declare-fun |real.div| (|REAL| |REAL|) |REAL|)
(assert (!
  (forall ((a |REAL|) (b |REAL|))
    (and
      (=> (and (<= 0 a) (< 0 b))
        (= (|real.div| a b) (/ a b)))
      (=> (and (<= 0 a) (< b 0))
        (= (|real.div| a b) (- (/ a (- b)))))
      (=> (and (< a 0) (< 0 b))
        (= (|real.div| a b) (- (/ (- a) b))))
      (=> (and (<= a 0) (< b 0))
        (= (|real.div| a b) (/ a b)))))
  :named |ax.real.div :1|))
(assert (!
    (not
      (= (|real.div| 5.0 2.0) 2.5))
    :named |Goal|))
(check-sat)
(exit)
