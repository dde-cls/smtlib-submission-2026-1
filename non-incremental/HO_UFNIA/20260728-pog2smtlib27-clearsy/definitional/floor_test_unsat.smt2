(set-info :smt-lib-version 2.7)
(set-logic HO_UFNIA)
(set-info :source "
Generated by: CLEARSY
Generated on: 2026-07-28
Generator: pog2smtlib-2.7
Application: floor_test
Target solver: cvc5
Time limit: 0.31
Benchmarks crafted to test the proof obligation translator pog2smtlib-2.7 for Atelier B.

MACHINE
input_unsat
CONSTANTS
co1,no1
PROPERTIES
co1:REAL &
no1:INT &
co1 = 2.5 &
no1 = 2
ASSERTIONS
floor(co1) = no1
END
")
(set-info :license "https://creativecommons.org/licenses/by/4.0/")
(set-info :category "crafted")
(set-info :status unsat)

;; type Z
(define-sort |Z| () Int)
;; Powerset constructor
(declare-sort P 1)
;; type POW Z
(define-sort |POW Z| () (P |Z|))
;; setmembership Z
(declare-fun |set.in Z| (|Z| |POW Z|) Bool)
;; type REAL
(define-sort |REAL| () Real)
;; set Z
(define-sort |? Z| () (-> |Z| Bool))
(declare-const |set.intent Z| (-> |? Z| |POW Z|))
(assert (!
  (forall ((p |? Z|))
    (forall ((x |Z|))
      (= (|set.in Z| x (|set.intent Z| p))
         (@ p x))))
  :named |ax:set.in.intent Z|))
;; MININT
(define-const MININT |Z| (- 2147483648))
;; MAXINT
(define-const MAXINT |Z| 2147483647)
;; free symbol no1
(declare-const no1 |Z|)
;; free symbol co1
(declare-const co1 |REAL|)
;; Int constructor
(declare-const INT |POW Z|)
(assert (!
(= INT (|set.intent Z| (lambda ((e |Z|)) (and (<= MININT e) (<= e MAXINT)))))
  :named |ax.set.in.INT|))
;; floor
(define-fun |real.floor| ((x |REAL|)) |Z| (to_int x))
(assert (!
  (|set.in Z| no1 INT)
  :named |Define:lprp:2|))
(assert (!
  (= co1 2.5)
  :named |Define:lprp:3|))
(assert (!
  (= no1 2)
  :named |Define:lprp:4|))
(assert (!
    (not
      (= (|real.floor| co1) no1))
    :named |Goal|))
(check-sat)
(exit)
