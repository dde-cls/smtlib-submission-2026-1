(set-info :smt-lib-version 2.7)
(set-logic HO_UFNIA)
(set-info :source "
Generated by: CLEARSY
Generated on: 2026-07-28
Generator: pog2smtlib-2.7
Application: rquantified_sum_test
Target solver: cvc5
Time limit: 0.31
Benchmarks crafted to test the proof obligation translator pog2smtlib-2.7 for Atelier B.

MACHINE
    input_unsat
ASSERTIONS
    SIGMA (xx) . ( xx : {2.5,5.5} |  xx ) = 8.0
END
")
(set-info :license "https://creativecommons.org/licenses/by/4.0/")
(set-info :category "crafted")
(set-info :status unsat)

;; type REAL
(define-sort |REAL| () Real)
;; Powerset constructor
(declare-sort P 1)
;; type POW REAL
(define-sort |POW REAL| () (P |REAL|))
;; setmembership REAL
(declare-fun |set.in REAL| (|REAL| |POW REAL|) Bool)
;; set REAL
(define-sort |? REAL| () (-> |REAL| Bool))
(declare-const |set.intent REAL| (-> |? REAL| |POW REAL|))
(assert (!
  (forall ((p |? REAL|))
    (forall ((x |REAL|))
      (= (|set.in REAL| x (|set.intent REAL| p))
         (@ p x))))
  :named |ax:set.in.intent REAL|))
;; {} REAL
(declare-const |set.empty REAL| |POW REAL|)
(assert (!
  (forall ((e |REAL|)) (not (|set.in REAL| e |set.empty REAL|)))
  :named |ax.set.in.empty REAL|))
;; SIGMA/REAL
(declare-fun |rSIGMA| (|POW REAL|) |REAL|)
(assert (!
  (= 0.0 (|rSIGMA| |set.empty REAL|))
  :named |ax.rsigma.empty|))
(assert (!
  (forall ((s |POW REAL|))
    (forall ((e |REAL|))
      (= (= (|rSIGMA| s) e)
         (or
           (and (= e 0.0)
                (= s |set.empty REAL|))
           (forall ((x |REAL|))
             (=> (|set.in REAL| x s)
                 (= e
                    (+ x
                       (|rSIGMA|
                         (|set.intent REAL|
                           (lambda ((y |REAL|))
                             (and (|set.in REAL| y s)
                                  (not (= y x))))))))))))))
  :named |ax.rsigma.incr|))
(assert (!
    (not
      (= (|rSIGMA| (|set.intent REAL| (lambda ((_c0 |REAL|)) (exists ((_c1 |REAL|)) (and     (|set.in REAL| _c1 (|set.intent REAL| (lambda ((_c2 |REAL|)) (or (= _c2 2.5)(= _c2 5.5))))) (= _c0 _c1)))))) 8.0))
    :named |Goal|))
(check-sat)
(exit)
