(set-info :smt-lib-version 2.7)
(set-logic HO_UFNIA)
(set-info :source "
Generated by: CLEARSY
Generated on: 2026-07-28
Generator: pog2smtlib-2.7
Application: rquantified_prod_test
Target solver: cvc5
Time limit: 0.31
Benchmarks crafted to test the proof obligation translator pog2smtlib-2.7 for Atelier B.

MACHINE
   input_unsat
ASSERTIONS
    PI(xx) . ( xx : {2.5,3.0} |  xx ) = 7.5
END
")
(set-info :license "https://creativecommons.org/licenses/by/4.0/")
(set-info :category "crafted")
(set-info :status unsat)

;; type REAL
(define-sort |REAL| () Real)
;; Powerset constructor
(declare-sort P 1)
;; type POW REAL
(define-sort |POW REAL| () (P |REAL|))
;; setmembership REAL
(declare-fun |set.in REAL| (|REAL| |POW REAL|) Bool)
;; set REAL
(define-sort |? REAL| () (-> |REAL| Bool))
(declare-const |set.intent REAL| (-> |? REAL| |POW REAL|))
(assert (!
  (forall ((p |? REAL|) (x |REAL|)) (!
    (= (|set.in REAL| x (|set.intent REAL| p))
       (@ p x))
    :pattern ((|set.in REAL| x (|set.intent REAL| p)))))
  :named |ax.rw.intent REAL|))
;; {} REAL
(declare-const |set.empty REAL| |POW REAL|)
(assert (!
    (= |set.empty REAL|
       (|set.intent REAL|
         (lambda ((e |REAL|)) false)))
    :named |ax.set.in.empty REAL|))
;; PI/REAL
(declare-fun |rPI| (|POW REAL|) |REAL|)
(assert (!
  (= 1.0 (|rPI| |set.empty REAL|))
  :named |ax.rpi.empty|))
(assert (!
  (forall ((s |POW REAL|))
    (forall ((e |REAL|))
      (! (= (= (|rPI| s) e)
           (or
             (and (= e 1.0)
                  (= s |set.empty REAL|))
             (forall ((x |REAL|))
               (=> (|set.in REAL| x s)
                   (= e
                      (* x
                         (|rPI|
                           (|set.intent REAL|
                             (lambda ((y |REAL|))
                               (and (|set.in REAL| y s)
                                    (not (= y x))))))))))))
        :pattern ((= (|rPI| s) e)))))
  :named |ax.rpi.incr|))
(assert (!
    (not
      (= (|rPI| (|set.intent REAL| (lambda ((_c0 |REAL|)) (exists ((_c1 |REAL|)) (and     (|set.in REAL| _c1 (|set.intent REAL| (lambda ((_c2 |REAL|)) (or (= _c2 2.5)(= _c2 3.0))))) (= _c0 _c1)))))) 7.5))
    :named |Goal|))
(check-sat)
(exit)
