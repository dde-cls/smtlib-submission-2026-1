(set-info :smt-lib-version 2.7)
(set-logic HO_UFNIA)
(set-info :source "
Generated by: CLEARSY
Generated on: 2026-07-28
Generator: pog2smtlib-2.7
Application: quantified_set_test
Target solver: cvc5
Time limit: 0.31
Benchmarks crafted to test the proof obligation translator pog2smtlib-2.7 for Atelier B.

MACHINE
    input_unsat
CONSTANTS
    co1, co2
PROPERTIES
    co1 = 0..100 &
    co2 = 0
ASSERTIONS
    co2: { xx | xx : INTEGER & xx : co1 }
END
")
(set-info :license "https://creativecommons.org/licenses/by/4.0/")
(set-info :category "crafted")
(set-info :status unsat)

;; type Z
(define-sort |Z| () Int)
;; Powerset constructor
(declare-sort P 1)
;; type POW Z
(define-sort |POW Z| () (P |Z|))
;; setmembership Z
(declare-fun |set.in Z| (|Z| |POW Z|) Bool)
;; set Z
(define-sort |? Z| () (-> |Z| Bool))
(declare-const |set.intent Z| (-> |? Z| |POW Z|))
(assert (!
  (forall ((p |? Z|) (x |Z|)) (!
    (= (|set.in Z| x (|set.intent Z| p))
       (@ p x))
    :pattern ((|set.in Z| x (|set.intent Z| p)))))
  :named |ax.rw.intent Z|))
;; free symbol co2
(declare-const co2 |Z|)
;; free symbol co1
(declare-const co1 |POW Z|)
;; ..
(declare-fun |interval| (|Z| |Z|) |POW Z|)
(assert (!
    (forall ((l |Z|) (u |Z|))
      (!
        (= (|interval| l u)
           (|set.intent Z|
             (lambda ((e |Z|))
               (and (<= l e)
                    (<= e u)))))
        :pattern ((|interval| l u))))
    :named |ax.set.in.interval|))
;; = Z
(assert (!
  (forall ((s |POW Z|) (t |POW Z|))
    (=
      (= s t)
      (forall ((e |Z|)) (= (|set.in Z| e s) (|set.in Z| e t)))))
  :named |ax.set.eq Z|))
(assert (!
  (= co1 (|interval| 0 100))
  :named |Define:lprp:1|))
(assert (!
  (= co2 0)
  :named |Define:lprp:2|))
(assert (!
    (not
      (|set.in Z| co2 (|set.intent Z| (lambda ((_c0 |Z|))     (and
      true
      (|set.in Z| _c0 co1))))))
    :named |Goal|))
(check-sat)
(exit)
