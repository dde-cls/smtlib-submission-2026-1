(set-info :smt-lib-version 2.7)
(set-logic HO_UFNIA)
(set-info :source "
Generated by: CLEARSY
Generated on: 2026-07-28
Generator: pog2smtlib-2.7
Application: rmax_test
Target solver: cvc5
Time limit: 0.31
Benchmarks crafted to test the proof obligation translator pog2smtlib-2.7 for Atelier B.

MACHINE
input_unsat
ASSERTIONS
max({2.5,5.5,0.0}) = 5.5
END
")
(set-info :license "https://creativecommons.org/licenses/by/4.0/")
(set-info :category "crafted")
(set-info :status unknown)

;; type REAL
(define-sort |REAL| () Real)
;; Powerset constructor
(declare-sort P 1)
;; type POW REAL
(define-sort |POW REAL| () (P |REAL|))
;; setmembership REAL
(declare-fun |set.in REAL| (|REAL| |POW REAL|) Bool)
;; set REAL
(define-sort |? REAL| () (-> |REAL| Bool))
(declare-const |set.intent REAL| (-> |? REAL| |POW REAL|))
(assert (!
  (forall ((p |? REAL|) (x |REAL|)) (!
    (= (|set.in REAL| x (|set.intent REAL| p))
       (@ p x))
    :pattern ((|set.in REAL| x (|set.intent REAL| p)))))
  :named |ax.rw.intent REAL|))
;; {} REAL
(declare-const |set.empty REAL| |POW REAL|)
(assert (!
    (= |set.empty REAL|
       (|set.intent REAL|
         (lambda ((e |REAL|)) false)))
    :named |ax.set.in.empty REAL|))
;; rmax
    (declare-fun |rmax| (|POW REAL|) |REAL|)
(assert (!
  (forall ((s |POW REAL|)) (!
    (=> (not (= s |set.empty REAL|)) (|set.in REAL| (|rmax| s) s))
    :pattern ( (|rmax| s) )))
  :named |ax.rmax.is.member|))
(assert (!
  (forall ((s |POW REAL|) (e |REAL|)) (!
      (=> (|set.in REAL| e s) (<= e (|rmax| s)))
    :pattern ( (|rmax| s) )))
  :named |ax.rmax.is.ge|))
(assert (!
    (not
      (= (|rmax| (|set.intent REAL| (lambda ((_c0 |REAL|)) (or (= _c0 2.5)(= _c0 5.5)(= _c0 0.0))))) 5.5))
    :named |Goal|))
(check-sat)
(exit)
